import 'package:flutter/material.dart';

class AdminColors {
  static const background = Color(0xFF1B1E2B);
  static const sidebar = Color(0xFF25293C);
  static const card = Color(0xFF2B3045);
  static const accentBlue = Color(0xFF0095FF);
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white60;
}
